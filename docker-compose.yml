services:
  redis:
    image: redis:7
    ports:
      - "6379:6379"
  api:
    build: .
    environment:
      - REDIS_URL=redis://redis:6379/0
      - RATE_LIMIT_MAX_PER_MIN=120
      - CACHE_TTL_SECONDS=60
    ports:
      - "8000:8000"
    depends_on:
      - redis
